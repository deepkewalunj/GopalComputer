import { AuthorizationCheck } from './authorization-check';

describe('AuthorizationCheck', () => {
  it('should create an instance', () => {
    expect(new AuthorizationCheck()).toBeTruthy();
  });
});
